# frozen_string_literal: true

RSpec.describe Sentry::Smart::Sampler do
  it "has a version number" do
    expect(Sentry::Smart::Sampler::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
